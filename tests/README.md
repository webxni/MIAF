# tests

Cross-service tests live here. Phase 0 has no tests yet — the sole acceptance criterion is that the compose stack boots and the `/health` endpoints return OK.

Phase 1 (Accounting Core) introduces real Python tests for the ledger.
