# packages/shared

Cross-package types and utilities shared between Python services and the TypeScript web app.

Empty in Phase 0. Populated as the contract between api/worker/scheduler/web grows (Phases 1+).
