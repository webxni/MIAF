name: miaf

networks:
  miaf:
    name: miaf_internal
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  minio_data:
  backup_data:
  caddy_data:
  caddy_config:
  web_node_modules:
  web_next_cache:

services:

  postgres:
    image: pgvector/pgvector:pg16
    container_name: miaf-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-miaf}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-miaf}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/docker/postgres/init.sql:/docker-entrypoint-initdb.d/00-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks: [miaf]

  redis:
    image: redis:7-alpine
    container_name: miaf-redis
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    command: ["sh", "-c", "redis-server --appendonly yes --requirepass \"$$REDIS_PASSWORD\""]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks: [miaf]

  minio:
    image: minio/minio:RELEASE.2024-12-13T22-19-12Z
    container_name: miaf-minio
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 20
    ports:
      # Dev only: bind MinIO console to localhost so the developer can browse buckets.
      # The S3 API (9000) stays internal-only.
      - "127.0.0.1:9001:9001"
    networks: [miaf]

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: dev
    image: miaf-api:dev
    container_name: miaf-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://${POSTGRES_USER:-miaf}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-miaf}}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET:-miaf}
      MINIO_SECURE: "false"
      CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
      AUTOMATION_TOKEN: ${AUTOMATION_TOKEN:-dev-automation-token}
    volumes:
      - ./apps/api:/srv/api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0) if urllib.request.urlopen('http://localhost:8000/health', timeout=2).status==200 else sys.exit(1)"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks: [miaf]

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
      target: dev
    image: miaf-web:dev
    container_name: miaf-web
    restart: unless-stopped
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
      # Bind dev server to all interfaces so Caddy can reach it
      HOSTNAME: 0.0.0.0
      PORT: "3000"
    volumes:
      - ./apps/web:/srv/web
      - web_node_modules:/srv/web/node_modules
      - web_next_cache:/srv/web/.next
    depends_on:
      - api
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:3000 >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 60s
    networks: [miaf]

  worker:
    build:
      context: .
      dockerfile: services/worker/Dockerfile
    image: miaf-worker:dev
    container_name: miaf-worker
    restart: unless-stopped
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-info}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://${POSTGRES_USER:-miaf}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-miaf}}
    volumes:
      - ./services/worker:/srv/worker
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks: [miaf]

  scheduler:
    build:
      context: .
      dockerfile: services/scheduler/Dockerfile
    image: miaf-scheduler:dev
    container_name: miaf-scheduler
    restart: unless-stopped
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-info}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://${POSTGRES_USER:-miaf}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-miaf}}
      HEARTBEAT_INTERVAL_SECONDS: ${HEARTBEAT_INTERVAL_SECONDS:-300}
      API_HEARTBEAT_URL: ${API_HEARTBEAT_URL:-http://api:8000/internal/heartbeat/run-defaults}
      AUTOMATION_TOKEN: ${AUTOMATION_TOKEN:-dev-automation-token}
    volumes:
      - ./services/scheduler:/srv/scheduler
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks: [miaf]

  caddy:
    image: caddy:2-alpine
    container_name: miaf-caddy
    restart: unless-stopped
    environment:
      CADDY_DOMAIN: ${CADDY_DOMAIN:-localhost}
      CADDY_ADMIN_EMAIL: ${CADDY_ADMIN_EMAIL:-}
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    volumes:
      - ./infra/docker/caddy/Caddyfile.dev:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
      - web
    networks: [miaf]

  backup:
    build:
      context: ./infra/docker/backup
    image: miaf-backup:dev
    container_name: miaf-backup
    restart: unless-stopped
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-miaf}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-miaf}
      BACKUP_INTERVAL_SECONDS: ${BACKUP_INTERVAL_SECONDS:-86400}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-7}
    volumes:
      - backup_data:/backups
    depends_on:
      postgres:
        condition: service_healthy
    networks: [miaf]
